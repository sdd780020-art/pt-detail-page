# 4STEPPT 히어로 첫 화면 전환 개선 — 반영 요청

작성일: 2026-08-26

히어로에 오퍼 뱃지를 신설하고, 스크롤 인디케이터가 첫 화면에서 보이지 않던 문제를 고치고, CTA 2종을 진입 즉시 노출합니다. 모바일 기준으로 설계했고 4개 뷰포트에서 실측 검증을 마쳤습니다.

| | |
|---|---|
| **반영 대상** | https://www.butfitground.com/4steppt |
| **참고 구현** | https://pt-detail-page.vercel.app/ — 아래 코드가 그대로 적용돼 동작 중 |
| **수정 범위** | 히어로 섹션 마크업 1곳 · CSS 11곳 · JS 1곳. 다른 섹션은 건드리지 않음 |

---

## 1. 왜 고치는지

스크롤 관여 분석 결과 **유입 100명 중 38명만 읽기 시작하고 5명만 끝까지 읽습니다.** CTA가 페이지 하단에만 있어서, 사실상 대부분의 방문자에게는 전환 수단이 없는 상태입니다.

그래서 **첫 화면 안에서 전환이 일어나도록** 오퍼·스크롤 힌트·CTA 세 요소를 폴드 위로 올립니다.

### 스크롤 인디케이터가 안 보이던 진짜 원인

화살표 색이 옅어서가 아니라 **애초에 화면 밖에 있었습니다.** `vh`는 iOS·Android 모두 주소창이 *접힌* 상태를 기준으로 계산되는데, 히어로 위에 sticky 헤더 60px과 LNB 51px, 합쳐서 **111px이 문서 흐름을 차지**합니다.

| iPhone 14 (390×844) | 계산 | 값 |
|---|---|---|
| 히어로 시작 위치 | 60 + 51 | 111px |
| 히어로 높이 | 92vh = 0.92 × 745 | 685px |
| 인디케이터 상단 | 111 + 685 − 26 − 45 | 725px |
| 실제 보이는 영역 | 주소창 펼침 (svh) | 659px |
| **→ 폴드보다 66px 아래** | | ✕ |

**`100svh`로만 바꿔도 해결되지 않습니다.** 111px를 빼줘야 합니다 — `calc(100svh - 111px)`.

---

## 2. 변경 사항

### A · HTML — 오퍼 뱃지 신설

혜택·가격·희소성을 첫 화면에서 즉시 보여주는 요소입니다. **`.hero__text` 블록 바깥**, `.hero__scroll` 바로 앞에 넣어주세요 — 텍스트에 붙이면 여백 균등 분배(변경 C)가 작동하지 않습니다.

```html
  </div>  <!-- .hero__text 끝 -->

  <p class="hero__offer">
    <span class="hero__offer-row hero__offer-tags">
      <span class="hero__offer-tag">생애 단 한번 체험권 혜택</span>
      <span class="hero__offer-tag hero__offer-tag--limit">매달 선착순 20명</span>
    </span>
    <span class="hero__offer-row">
      <span class="hero__offer-body">PT 2회 + 피트니스 7일 + 내 몸 변화 설계서</span>
    </span>
    <span class="hero__offer-row hero__offer-price">
      <s class="was">176,000원</s>
      <span class="now">49,900<span class="won">원</span></span>
      <span class="rate">72%<i aria-hidden="true">↓</i></span>
    </span>
  </p>

  <div class="hero__scroll" aria-hidden="true"><i></i><i></i><i></i></div>
```

> **⚠️ 카피 확정본 — 임의 수정 금지**
>
> **"매달"**(매월 아님), **"생애 단 한번 체험권 혜택"**, 구성 3종은 **"PT 2회 + 피트니스 7일 + 내 몸 변화 설계서"**로 모두 동일 위계입니다. 설계서를 작은 글씨 부가정보로 빼지 말아주세요.
>
> 가격은 결제 페이지 실값입니다 — 정가 176,000원 / 72% 할인 / 49,900원.

### B · CSS — 전역 토큰 2개 추가

플로팅 CTA가 화면 하단에서 점유하는 높이입니다. **이후 모든 변경이 이 값을 참조하므로 가장 먼저 넣어주세요.**

```css
:root {
  /* 플로팅 CTA가 점유하는 하단 밴드 (버튼 52 + bottom 16 + 여유 16) */
  --float-h: 84px;
  --float-safe: calc(var(--float-h) + env(safe-area-inset-bottom, 0px));
}
```

### C · CSS — 히어로 높이 재정의 + 여백 균등 분배

높이는 **sticky 헤더·LNB를 뺀 실제 가용 영역**으로 잡고, 남는 세로 공간은 **auto 마진 3개가 자동으로 3등분**합니다. 덕분에 "카피 → 뱃지" 간격과 "뱃지 → 화살표" 간격이 기기 높이와 무관하게 항상 같아집니다.

```css
.hero {
  /* 삭제
  min-height: 92vh;
  justify-content: center;
  padding: 96px var(--sp-lg) var(--sp-4xl);
  */

  /* sticky 헤더 60 + LNB 51 — 이만큼이 히어로 위 flow를 차지한다 */
  --hero-chrome: 111px;
  /* 하단 스택: 플로팅 CTA + 갭 + 스크롤 인디케이터(44) */
  --hero-stack: calc(var(--float-safe) + 16px + 44px);
  min-height: 72vh;                              /* svh 미지원(iOS<15.4) 폴백 */
  min-height: calc(100svh - var(--hero-chrome));
  justify-content: flex-start;
  padding: var(--sp-base) var(--sp-lg) var(--hero-stack);
}

/* 남는 세로 공간을 auto 마진 3개가 균등 분배 */
.hero__text  { margin-top: auto; }
.hero__offer { margin-top: auto; margin-bottom: auto; }
```

> **시행착오 공유**
>
> 처음엔 `justify-content:center`를 유지한 채 padding으로 위치를 맞추려 했는데 **역효과였습니다** — 아래 padding을 키우면 중앙정렬 때문에 텍스트가 위로 밀려 간격이 오히려 벌어집니다(35→42px). 기기별로 값을 맞추는 방식도 히어로 높이가 달라 성립하지 않습니다. auto 마진이 유일하게 깔끔한 해법이었습니다.

### D · CSS — 오퍼 뱃지 스타일 (신규)

3행 가로형입니다. 각 행이 `flex-wrap` 컨테이너라 **폭이 모자라면 어절 단위로 알아서 접힙니다** — 문구가 길어져도 글자가 어색하게 잘리지 않습니다.

`.hero__sub` 규칙 다음에 추가:

```css
.hero__offer {
  position: relative; z-index: 1;
  display: flex; flex-direction: column; align-items: center; gap: 7px;
  max-width: 100%;
  padding: 11px var(--sp-lg) 13px;
  border-radius: var(--r-2xl);
  background: rgba(13, 13, 14, .5);
  border: 1px solid var(--brand-line);
  backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
  box-shadow: 0 10px 34px rgba(0, 0, 0, .38);
  text-align: center;
  opacity: 0; animation: fadeUp .6s cubic-bezier(.16,1,.3,1) .36s forwards;
}

/* 한 행 안의 조각들 — 조각은 안 쪼개지고, 행이 좁으면 조각 단위로 wrap */
.hero__offer-row {
  display: flex; flex-wrap: wrap; align-items: baseline;
  justify-content: center; gap: 3px 8px;
}
.hero__offer-row > * { white-space: nowrap; }

.hero__offer-tags { gap: 4px 7px; }
.hero__offer-tag {
  padding: 3px 10px; border-radius: var(--r-pill);
  background: var(--brand); color: #06210F;
  font-size: 11px; font-weight: 800; line-height: 1.55; letter-spacing: -.3px;
  white-space: nowrap;
}
/* 희소성 배지 — 그린 pill 옆에서 톤을 낮춰 두 라벨이 서로 잡아먹지 않게 */
.hero__offer-tag--limit {
  background: rgba(255, 255, 255, .15);
  color: rgba(255, 255, 255, .94);
}

/* 구성 3종 동일 위계 — 좁은 폭에서 word-break:keep-all이 어절 단위로 접는다 */
.hero__offer-body {
  font-size: 15px; font-weight: 700; line-height: 1.4; letter-spacing: -.4px;
  color: #FFFFFF;
  white-space: normal;
}

/* 가격 — 정가(취소선) / 판매가 / 할인율 순 */
.hero__offer-price { gap: 2px 9px; margin-top: 1px; font-feature-settings: "tnum" 1; }
.hero__offer-price .was {
  font-size: 12.5px; font-weight: 600; letter-spacing: -.3px;
  color: rgba(255, 255, 255, .42); text-decoration-thickness: 1px;
}
.hero__offer-price .now {
  font-size: 23px; font-weight: 800; line-height: 1.2; letter-spacing: -1px;
  color: var(--brand);
}
.hero__offer-price .now .won { font-size: 14.5px; font-weight: 700; }
.hero__offer-price .rate {
  font-size: 14.5px; font-weight: 800; letter-spacing: -.3px; color: var(--brand);
}
.hero__offer-price .rate i { font-style: normal; font-size: 12px; margin-left: 1px; }
```

토큰 값 참고 (없으면 직접 넣어주세요):
`--brand` = `#00D857` / `--brand-line` = `rgba(0,216,87,.32)` / `--r-2xl` = `28px` / `--r-pill` = `999px` / `--sp-lg` = `20px`

### E · CSS — 스크롤 인디케이터 재배치 + 대비 강화

기존 `bottom: 26px`은 상시 노출된 플로팅 CTA 밴드(16~102px) **한복판**이라 완전히 가려집니다. 대비도 실효 알파 **0.083 → 0.32**로 약 4배 올립니다.

```css
.hero__scroll {
  /* 삭제: bottom: 26px;  gap: 3px; */
  bottom: calc(var(--float-safe) + var(--sp-base));  /* 플로팅 CTA 바로 위 */
  gap: 4px;
  pointer-events: none;                              /* CTA 근처 탭을 가로채지 않게 */
  filter: drop-shadow(0 2px 4px rgba(0,0,0,.55));    /* 사진 위 대비 확보 */
}

.hero__scroll i {
  /* 삭제
  width: 13px; height: 13px;
  border-right: 2px solid rgba(255,255,255,.55);
  border-bottom: 2px solid rgba(255,255,255,.55);
  */
  width: 12px; height: 12px;
  border-right: 2.5px solid rgba(255,255,255,.92);
  border-bottom: 2.5px solid rgba(255,255,255,.92);
}

@keyframes chev {
  /* 삭제: 0%,100% { opacity: .15 }  /  50% { opacity: .9 } */
  0%, 100% { opacity: .35; transform: rotate(45deg) translate(0, 0); }
  50%      { opacity: 1;   transform: rotate(45deg) translate(3px, 3px); }
}
```

### F · CSS + JS — 플로팅 CTA를 첫 화면부터 상시 노출

기존엔 히어로가 보이는 동안 숨겨져 있어서, 스크롤을 해야 CTA가 나타났습니다. **기본값을 "노출"로 뒤집고 숨김을 `.is-hidden`으로** 바꿉니다 — 이러면 JS 로드 전이나 로드 실패 시에도 CTA가 보입니다.

```css
/* 기본 (모바일) */
.float-cta {
  /* 삭제
  transform: translateY(140%); opacity: 0;
  pointer-events: none;
  */
  transform: translateY(0); opacity: 1; pointer-events: auto;
}
/* 삭제: .float-cta.is-visible { transform: translateY(0); opacity: 1; pointer-events: auto; } */
.float-cta.is-hidden { transform: translateY(140%); opacity: 0; pointer-events: none; }


/* 데스크탑 @media (min-width: 1024px) 블록 안 */
/* 삭제
  .float-cta { transform: translateX(-50%) translateY(140%); }
  .float-cta.is-visible { transform: translateX(-50%) translateY(0); }
*/
  .float-cta { left: 50%; right: auto; transform: translateX(-50%); }
  .float-cta.is-hidden { transform: translateX(-50%) translateY(140%); }
```

JS — 플로팅 CTA 블록 전체 교체:

```js
// 첫 화면부터 상시 노출(CSS 기본값). 최종 CTA 섹션에서만 숨겨 버튼 중복을 피한다.
const floatCta = document.getElementById('floatCta');
const ctaSection = document.getElementById('cta');

if (floatCta && ctaSection) {
  new IntersectionObserver((entries) => {
    entries.forEach((e) => floatCta.classList.toggle('is-hidden', e.isIntersecting));
  }, { threshold: 0.25 }).observe(ctaSection);
}
```

히어로를 감시하던 IntersectionObserver와 `heroSection` 변수는 삭제합니다. 최종 CTA 섹션에서 숨는 동작은 그대로 유지됩니다(버튼 중복 방지).

### G · CSS — 나머지 보정 4건

뱃지가 들어오면서 세로 공간이 빠듯해진 것과, CTA 상시 노출로 생긴 부수 영향을 정리합니다.

```css
/* 1. 헤드라인 하한 40px → 36px
      360px 단말에서 "진짜 나를 위한 수업,"이 3줄로 터지던 기존 버그 */
.hero__title { font-size: clamp(36px, 7.5vw, 82px); }

/* 2. 타이포 여백 미세 조정 */
.hero__kicker { line-height: 1.5; margin-bottom: var(--sp-sm); }
.hero__sub    { margin-top: var(--sp-base); line-height: 1.55; }

/* 3. iPhone SE 등 세로 아주 짧은 단말에서만 서브카피를 접는다.
      ⚠️ 임계값을 700px로 잡으면 주소창 펼친 iPhone 14(svh 659)까지 걸린다 */
@media (max-height: 600px) {
  .hero { padding-top: var(--sp-sm); }
  .hero__sub { display: none; }
}

/* 4. 푸터 — 고정 110px이면 노치 단말에서 저작권 줄이 CTA 바에 스친다 */
.footer { padding: var(--sp-4xl) 0 calc(var(--float-safe) + var(--sp-3xl)); }

/* 5. 모션 최소화 설정 대응 */
@media (prefers-reduced-motion: reduce) {
  .hero__offer    { animation: none; opacity: 1; }
  .hero__scroll i { animation: none; opacity: 1; }
  .float-cta      { transition: none; }
}

/* 6. 데스크탑 — 뱃지를 한 줄 알약으로 */
@media (min-width: 1024px) {
  .hero {
    --hero-chrome: 129px;              /* 헤더 72 + LNB 57 */
    min-height: calc(100vh - var(--hero-chrome));
    padding-top: var(--sp-4xl);
  }
  .hero__offer {
    flex-direction: row; align-items: center; gap: var(--sp-md);
    padding: 8px var(--sp-lg) 8px 10px;
    border-radius: var(--r-pill);
  }
  .hero__offer-price .now { font-size: 24px; }
  .hero__offer-price .was { font-size: 13px; }
}
```

---

## 3. 적용 순서

토큰이 다른 변경의 선행 조건이라 순서가 있습니다.

1. **B** — `:root` 토큰 2개. 나머지 전부가 이 값을 참조합니다.
2. **F** — 플로팅 CTA 기본값 반전(CSS) + JS 옵저버 교체.
3. **C · E** — 히어로 박스와 인디케이터. 이 시점에 화살표가 첫 화면에 들어옵니다.
4. **A · D** — 뱃지 마크업과 스타일.
5. **G** — 보정 4건.

---

## 4. 검증 기준

**모바일 첫 랜딩에서 스크롤 없이 ① 오퍼 뱃지 ② 스크롤 화살표 ③ CTA 2종이 동시에 보이면 성공입니다.**

### 참고 구현 실측값

Edge 실제 렌더 기준. 모바일 높이는 주소창을 펼친 `svh` 값입니다.

| 뷰포트 | 3요소 노출 | 카피→뱃지 | 뱃지→화살표 | 차이 |
|---|---|---|---|---|
| iPhone 14 · 390×659 | PASS | 27.9px | 28.0px | 0.1px |
| iPhone SE · 375×553 | PASS | 17.1px | 17.2px | 0.1px |
| Galaxy S · 360×640 | PASS | 21.6px | 21.6px | 0.0px |
| Desktop · 1440×900 | PASS | 76.7px | 76.7px | 0.0px |

### 체크리스트

- [ ] iOS Safari에서 **주소창을 펼친 최초 진입 상태**로 세 요소가 모두 보이는지 (가장 빡빡한 조건입니다)
- [ ] 360px 단말에서 헤드라인이 2줄인지 (3줄이면 `clamp` 하한이 안 먹은 것)
- [ ] 뱃지 각 행이 1줄로 들어가는지
- [ ] 스크롤 화살표와 CTA 버튼이 겹치지 않는지
- [ ] 최종 CTA 섹션에 도달하면 플로팅 바가 사라지고, 위로 올라오면 다시 뜨는지
- [ ] 노치 단말에서 푸터 저작권 줄이 CTA 바에 가리지 않는지

> **⚠️ 확인 부탁드립니다**
>
> `--hero-chrome: 111px`는 헤더 60px + LNB 51.1px 실측값입니다. 4steppt의 헤더·LNB 높이가 다르면 **그 합으로 바꿔주세요.** 콘솔에서 `document.querySelector('.lnb').getBoundingClientRect().height`로 확인할 수 있습니다.

---

## 5. 함께 논의하고 싶은 것

- **"체험권 구매하기" 버튼의 결제 URL** — 참고 구현에서는 아직 `href="#"`입니다. 4steppt에 실제 결제 링크가 연결돼 있다면 그대로 두시면 됩니다.
- **"매달 선착순 20명" 운영 확인** — 첫 화면에 노출되는 문구라 실제 운영 정책과 어긋나면 안 됩니다.

---

참고 구현은 https://pt-detail-page.vercel.app/ 에서 실제 동작을 확인하실 수 있습니다.
코드 전문이나 다른 뷰포트 캡처가 필요하시면 말씀해주세요.
